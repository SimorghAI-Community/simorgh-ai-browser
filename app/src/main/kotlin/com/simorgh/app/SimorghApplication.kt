package com.simorgh.app

import android.app.Application

/**
 * Composition root placeholder.
 *
 * DI graph wiring (Hilt) is added when core:data / core:ai-engine
 * implementations exist. See DEVELOPMENT_LOG.md — Phase 0.
 */
class SimorghApplication : Application()
